{
  "name": "zns",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create": "graph create --node http://localhost:8020/ arthera/zns-subgraph",
    "remove": "graph remove --node http://localhost:8020/ arthera/zns-subgraph",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arthera/zns-subgraph --network mainnet --version-label 0.0.1",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
